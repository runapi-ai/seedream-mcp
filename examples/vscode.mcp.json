{
  "servers": {
    "seedream": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/seedream-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
